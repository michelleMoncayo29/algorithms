/**
 * Count Vowels Exercise
 * 
 * Implementa la función countVowels que cuente el número de vocales en una string.
 * Las vocales son: a, e, i, o, u (tanto en mayúsculas como minúsculas).
 */

/**
 * Cuenta el número de vocales en una string
 * @param {string} str - La string a analizar
 * @returns {number} - El número de vocales encontradas
 */
function countVowels(str) {
    // TODO: Implementa esta función
    // Pista: Itera sobre cada carácter de la string
    // Convierte cada carácter a minúscula para facilitar la comparación
    // Verifica si el carácter es una vocal (a, e, i, o, u)
    // Mantén un contador de vocales encontradas
    
    return 0;
}

module.exports = countVowels;
